# Project instructions

Use Node.js 24 and npm. Run `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build` before delivery. Never read or commit dotenv files. Keep inventory data in structured `properties` queries; use RAG only for stable knowledge. Validate Meta webhook signatures before processing messages.
